{{- if .Values.auth.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "swarndb.fullname" . }}
  labels:
    {{- include "swarndb.labels" . | nindent 4 }}
type: Opaque
data:
  api-keys: {{ join "," .Values.auth.apiKeys | b64enc | quote }}
{{- end }}
