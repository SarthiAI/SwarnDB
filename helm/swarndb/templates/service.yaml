apiVersion: v1
kind: Service
metadata:
  name: {{ include "swarndb.fullname" . }}
  labels:
    {{- include "swarndb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
    - port: {{ .Values.service.restPort }}
      targetPort: rest
      protocol: TCP
      name: rest
  selector:
    {{- include "swarndb.selectorLabels" . | nindent 4 }}
