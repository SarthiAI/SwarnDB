apiVersion: v1
kind: Secret
metadata:
  name: swarndb-secret
  namespace: swarndb
  labels:
    app.kubernetes.io/name: swarndb
    app.kubernetes.io/component: config
  annotations:
    # RECOMMENDED: Use an external secret manager instead of storing secrets in version control.
    # Options: Sealed Secrets, External Secrets Operator, HashiCorp Vault.
    # external-secrets.io/provider: "vault"
    # external-secrets.io/vault-path: "secret/data/swarndb"
type: Opaque
data:
  # WARNING: Replace this placeholder before deploying. Never commit real API keys.
  # Generate with: echo -n "your-key-1,your-key-2" | base64
  SWARNDB_API_KEYS: "Q0hBTkdFX01FX0JFRk9SRV9ERVBMT1k="  # base64("CHANGE_ME_BEFORE_DEPLOY")
